# ─── Authentication (choose ONE) ───────────────────────────────
# The agent uses whichever it finds, in this priority order.

# Option A — standard Anthropic API key (pay-per-token). Recommended.
# Get one at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# Option B — OAuth token from a Claude Pro/Max subscription.
# Generate with:  claude setup-token
# Then paste it here and leave ANTHROPIC_API_KEY blank.
CLAUDE_CODE_OAUTH_TOKEN=

# ─── Optional ───────────────────────────────────────────────────
# Override the model (defaults to claude-opus-4-8)
# COPYWRITER_MODEL=claude-opus-4-8
